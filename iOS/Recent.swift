import SwiftUI

struct Recent: View {
    let session: Session
    
    var body: some View {
        List {
            
        }
        .listStyle(.insetGrouped)
        .navigationTitle("Recent")
        .navigationBarTitleDisplayMode(.large)
    }
}
